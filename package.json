{
  "name": "aris",
  "version": "0.1.0-alpha",
  "description": "Aris - Desktop AI companion with HUD visual and INFP personality",
  "main": "electron.main.js",
  "scripts": {
    "build:renderer": "node build.renderer.js",
    "start": "npm run build:renderer && electron .",
    "build": "npm run build:renderer && electron-builder --config electron-builder.config.js"
  },
  "keywords": [
    "electron",
    "aris",
    "ai",
    "companion"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "electron": "^28.3.3",
    "electron-builder": "^24.9.1",
    "esbuild": "^0.19.0"
  },
  "dependencies": {
    "@lancedb/lancedb": "^0.26.0",
    "dotenv": "^16.4.5",
    "jsonrepair": "^3.13.2",
    "sql.js": "^1.10.0",
    "three": "^0.160.0"
  }
}
